name: Convert YOLO Model to ONNX

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  convert:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
        pip install requests ultralytics onnxruntime doclayout_yolo huggingface_hub

    - name: Convert model to ONNX
      env:
          MODEL_URL: https://huggingface.co/opendatalab/PDF-Extract-Kit-1.0/resolve/main/models/Layout/YOLO/doclayout_yolo_ft.pt?download=true
      run: python convert_onnx.py

    - name: Upload ONNX artifact
      uses: actions/upload-artifact@v4
      with:
        name: converted-model
        path: doclayout_yolo_ft.onnx
